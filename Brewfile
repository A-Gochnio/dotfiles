# Installed by install.zsh via `brew bundle`.
# Core CLI tools referenced by the dotfiles themselves:
brew "gnu-sed"
brew "nano"          # link/.nanorc includes brew-nano syntax files; macOS "nano" is pico
brew "tmux"
brew "zsh-completions"

# Everyday tools
brew "gh"
brew "git-lfs"
brew "htop"
brew "jq"
brew "wget"

# Work: AWS + k8s (used by 61_LOCAL_aliases)
brew "awscli"
brew "k9s"
brew "kubectx"
cask "aws-vault-binary"

# Present on the old machine — uncomment what the new one needs:
# brew "cmake"
# brew "git-filter-repo"
# brew "imagemagick"
# brew "links"
# brew "telnet"
# brew "displayplacer"
# cask "macfuse"
